# EXP-BUG-066 — Citation sources could not be opened safely

The response omitted canonical URLs and the drawer offered no source action.
Canonical URLs are now additive response fields and the UI opens only HTTP(S)
targets with `noopener noreferrer`. Regression and mutation evidence is in M10.

