[pytest]
# No TestLookup-specific options needed — the plugin auto-registers when it
# finds testlookup.properties in the cwd. Tune pytest as you would normally.
addopts = -v --tb=short
testpaths = .
