# JR-04 — AI Report Journey

Status: **not started.**

Its precondition is a terminal pipeline from JR-03, and JR-03 produced none —
Celery beat could not publish to Redis on the local stack, so no pipeline ran.
Every step of JR-04 reads a report that does not exist:

| Step | Depends on |
|---|---|
| 1. report renders for completed / `partial` / failed pipelines | a pipeline in each state |
| 2. every cited failure exists in the run; counts re-derivable | a generated report |
| 3. clusters cover all failures, or an explicit "N unclustered" | the clustering stage |
| 4. edge cases: 0 failures, 1 failure, all-fail, only-skips, no stack trace, 2 MB log line | six more ingests plus pipelines |
| 5. provenance: each conclusion names its evidence and the agent | a generated report |

Nothing here is a statement about the AI report's quality. It was not examined.

The adjacent surface `/intelligence` **was** measured — in the previous program
(TL-2026-09-18-01-007, table geometry) and re-verified live on the homelab during
this session — but that is layout, not report correctness.

## To start

Restore beat to Redis, run JR-03 steps 1-6, then JR-04 becomes measurable.
Step 4's edge cases need six purpose-built sample files; none were written.
