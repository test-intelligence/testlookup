# Auto-generated by scripts/quality_gate.py --update-baseline.
# Each line is a tolerated <relpath>:<lineno> match for this guard.
# Reviewed in PR; remove entries as the code is cleaned up.
#
# EMPTY ON PURPOSE. Every structlog call in backend/app now uses kwargs, and
# the guard is AST-based so a call split across lines can no longer slip past
# it. A new entry here should be treated as a mistake, not a workflow step:
# this bug class silently disables whatever feature encloses it.
